# Embedded Postgres (default) — files stored in ./data/facerights
DATABASE_URL=pglite://./data/facerights
API_PORT=3001

# Switch to a Postgres server (Docker or local install):
# DATABASE_URL=postgres://facerights:facerights@localhost:5432/facerights
